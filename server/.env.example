# ==========================================
# 🔥 环境变量配置文件
# ==========================================
# 复制此文件为 .env 并填入实际值

# 服务器配置
PORT=3000
NODE_ENV=development

# 数据库配置
DB_HOST=
DB_USER=
DB_PASSWORD=
DB_NAME=
DB_CONNECTION_LIMIT=10

# JWT 配置（⚠️ 生产环境务必修改为复杂的密钥）
JWT_SECRET=your-super-secret-jwt-key-please-change-this-in-production-use-random-string
JWT_EXPIRES_IN=7d

# CORS 配置（允许的前端域名，用逗号分隔）
ALLOWED_ORIGINS=http://localhost:5173,http://localhost:3000

# 日志配置
LOG_LEVEL=info

# 文件上传配置
MAX_FILE_SIZE=10485760

# ==========================================
# 🔒 安全提示
# ==========================================
# 1. 不要将 .env 文件提交到 Git 仓库
# 2. JWT_SECRET 应使用 32 位以上的随机字符串
# 3. 生产环境中 DB_PASSWORD 必须使用强密码
# 4. ALLOWED_ORIGINS 应设置为实际的前端域名
# ==========================================